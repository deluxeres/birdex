// Все числа экономики — только здесь. В UI ничего не хардкодим.
// Когда появится бэкенд, этот конфиг будет приходить с сервера.

export const ECONOMY = {
  /** Сколько монет дают за одно яйцо на рынке. */
  eggSellPrice: 5,
  /** Максимум часов оффлайн-производства. */
  offlineCapHours: 8,
  /**
   * Склад яиц прокачивается за монеты: 1 000 → 50 000 000 за 29 улучшений
   * (вместимость ×1.45 за уровень). Полный склад — ферма перестаёт копить.
   * Цена улучшения = новая вместимость × costPerEgg × costGrowth^уровень:
   * первое 700, дальше склад стоит ~10–25% от всех трат игрока.
   */
  storage: {
    start: 1000,
    max: 50_000_000,
    levels: 30,
    costPerEgg: 0.466,
    costGrowth: 1.05,
  },
  /** Стартовые монеты нового игрока. */
  startCoins: 1000,
  /** Смена названия фермы: первая бесплатно, каждая следующая — столько монет. */
  renameCost: 1000,
  /** % с продажи яиц друга, который получает пригласивший (сервер: REF_SHARE в apps/api). */
  referralPercent: 12,

  /** Курицы: максимум уровня и рост за уровень. */
  chickenMaxLevel: 15,
  /** +20% к текущему доходу за каждое улучшение. */
  productionGrowth: 1.2,
  /** Цена улучшения растёт в 1.37 раза за уровень (прокачка долгая). */
  upgradeGrowth: 1.37,
  /** Цена первого улучшения = яиц/час × это число (≈ 12 ч окупаемости). */
  upgradeCostPerEggHour: 12,

  energy: {
    /** Стартовый максимум энергии. */
    start: 300,
    /** +энергии за одно улучшение. */
    upgradeStep: 50,
    /** Потолок энергии. */
    max: 1000,
    /**
     * Прокачка энергии (у каждого режима своя, цены одинаковые): каждый уровень +50 к максимуму.
     * 300 → 1000 = 14 уровней. Первые 5 цен заданы, дальше примерно ×3 за уровень.
     */
    upgradeCosts: [
      2_000, 10_000, 40_000, 130_000, 400_000,
      1_200_000, 3_600_000, 11_000_000, 33_000_000, 100_000_000,
      300_000_000, 900_000_000, 2_700_000_000, 8_000_000_000,
    ] as readonly number[],
    /** За сколько часов энергия восстанавливается с нуля до максимума. */
    refillHours: 8,
    /** Цена одной попытки "Ловли яиц". На 300 энергии — 6 попыток, на 1000 — 20. Покупки энергии нет — только прокачка. */
    playCost: 50,
  },

  play: {
    /** Длительность одной попытки ловли яиц. */
    durationSeconds: 45,
    /** Жизней в попытке. Пропустил яйцо — минус жизнь. */
    lives: 3,
    goldenChance: 0.035,
    /** Большая часть падающих объектов — мусор. Клик по мусору снимает жизнь. */
    trashChance: 0.68,
    normalReward: 1,
    /** Золотое яйцо = 5 яиц. */
    goldenReward: 5,
    /** Ледяное яйцо: раз в iceEveryMs с шансом iceChance. Поймал — всё замедляется. */
    iceEveryMs: 5000,
    iceChance: 0.1,
    /** Во сколько раз всё медленнее во время заморозки (1.1 = на 10% медленнее). */
    iceSlowFactor: 1.1,
    iceSlowSeconds: 10,
    /** Ледяное яйцо не даёт яиц, и за его пропуск жизнь не теряется. */
    iceReward: 0,
    /**
     * Сложность растёт с каждым пойманным яйцом (n):
     *   яиц в секунду = spawnStart + n × spawnPerEgg (до spawnMax)
     *   время падения = fallStartMs − n × fallPerEggMs (не меньше fallMinMs)
     * Поле заполнено объектами: старт медленный, дальше темп постепенно ускоряется.
     */
    spawnStart: 3.4,
    spawnPerEgg: 0.035,
    spawnMax: 8,
    fallStartMs: 5500,
    fallPerEggMs: 34,
    fallMinMs: 1550,
    /** Окно между ловлями, чтобы комбо не сбросилось, мс (пока только визуально). */
    comboWindowMs: 900,
    comboSteps: [1, 2, 3, 5] as readonly number[],
    comboEvery: 5,
    /** Анти-чит: максимум яиц в секунду за попытку. */
    maxEggsPerSecond: 6,
    /** Анти-чит: попытка не длиннее этого (защита от "вечной" сессии). */
    maxSessionMinutes: 15,
  },
  /**
   * Режимы Play кроме "Ловли яиц". У каждого своя энергия: старт 300, прокачка как у основной
   * (energy.upgradeCosts, +50 до 1000), полное восстановление за energy.refillHours.
   */
  modes: {
    /** Защита от лис: лисы бегут к яйцу в центре, тапаешь — убиваешь. */
    fox: {
      playCost: 75,
      /** Жизней у яйца: каждая лиса, добежавшая до яйца, кусает на одну. */
      lives: 3,
      /** Обычная лиса: 1 тап = 1 яйцо. */
      foxReward: 1,
      /** Плотная лиса: нужно 3 тапа, даёт 5 яиц. */
      tankHits: 3,
      tankReward: 5,
      /** Шанс плотной лисы: растёт от start до max за tankRampSeconds. */
      tankChanceStart: 0.06,
      tankChanceMax: 0.35,
      tankRampSeconds: 120,
      /** Скорость лис (px/с): start + perSecond × время игры, до max ("адская" к концу). */
      speedStart: 90,
      speedPerSecond: 2.2,
      speedMax: 320,
      /** Пауза между лисами (с): start × decay^(время/10), не меньше min. */
      spawnStart: 1.07,
      spawnDecay: 0.86,
      spawnMin: 0.22,
      /** Анти-чит: больше стольких убийств в секунду не засчитываем. */
      maxKillsPerSecond: 5,
    },
    /** Курица и бомбы: ведёшь курицу пальцем мимо падающих бомб, живёшь как можно дольше. */
    /** Chicken Flight (режим run): за каждый запуск тратится энергия режима. */
    run: {
      playCost: 30,
      /** Награда: столько яиц за минуту выживания (считается по секундам). */
      eggsPerMinute: 20,
      /** Продержался столько минут — победа и главный приз вместо поминутной награды. */
      maxMinutes: 10,
      winPrize: 500,
      /** Бомбы (с между бомбами): start − perSecond × время, не меньше min. Без хардкора. */
      spawnStart: 1.1,
      spawnPerSecond: 0.0013,
      spawnMin: 0.34,
      /** Скорость падения бомб (px/с). */
      fallStart: 170,
      fallPerSecond: 0.36,
      fallMax: 400,
    },
    /** Дабл: барабан на 38 слотов, ставка яйцами на цвет. За каждый спин — энергия режима. */
    double: {
      playCost: 30,
      minAmount: 25,
      maxAmount: 10_000,
      presets: [25, 50, 100, 250] as readonly number[],
      /** Во сколько раз умножается ставка при выигрыше (ставка входит в выплату). */
      payout: { red: 2, black: 2, green: 14 },
      historyLimit: 12,
    },
  },

  /**
   * Уровень игрока = сколько монет он потратил на КУРИЦ (1 монета = 1 XP):
   * покупка новой курицы и улучшение куриц. Склад и энергия опыта не дают.
   * Пороги уровней распределены по всей стоимости куриц (симуляция в economy/progression).
   */
  player: {
    maxLevel: 50,
    /** Сколько нужно потратить для 2-го уровня. */
    level2Xp: 600,
  },

  /**
   * Ежедневная награда: 28 дней = 4 недели по 7 (листаются стрелками в календаре).
   * Каждый 7-й день — подарок. Пропустил день — снова с первого дня.
   */
  rewardStreak: [
    100, 150, 250, 400, 600, 900, 2_000,
    1_200, 1_500, 1_800, 2_200, 2_600, 3_000, 6_000,
    3_500, 4_000, 4_600, 5_200, 5_900, 6_600, 12_000,
    10_000, 12_500, 15_000, 20_000, 30_000, 35_000, 40_000,
  ] as readonly number[],
  /**
   * Часовой пояс ежедневной награды: новый день начинается в 00:00 по Вашингтону.
   * Забрать можно 1 раз за календарный день.
   */
  rewardTimeZone: 'America/New_York',
  /** Пропустил целый день — серия сбрасывается на День 1. */
  rewardResetOnMiss: true,
} as const
